import 'package:flutter/material.dart';

@immutable
class ChapterContentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    Theme(data: ThemeData(), child: null);
    return Container();
  }
}
